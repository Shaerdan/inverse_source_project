# Inverse Source Localization - Pip Requirements
# ================================================
#
# For minimal installation (conformal/analytical solvers only):
#     pip install -r requirements.txt
#
# For full installation:
#     pip install -r requirements.txt
#     pip install -r requirements-full.txt
#
# NOTE: Some dependencies are better installed via conda:
#     - cyipopt: conda install -c conda-forge cyipopt
#     - FEniCSx: conda install -c conda-forge fenics-dolfinx
#
# ================================================

# Core Scientific Stack (REQUIRED)
numpy>=1.24
scipy>=1.11
matplotlib>=3.7

# Convex optimization for L1/TV regularization
cvxpy>=1.4
ecos>=2.0

# Clustering for source detection
scikit-learn>=1.3

# Mesh handling (for FEM/BEM)
gmsh>=4.12
meshio>=5.3
h5py>=3.9

# Utilities
tqdm
pyyaml

# Visualization (optional but recommended)
pyvista>=0.42

# Development (optional)
# pytest>=7.0
# black
# flake8
