# Inverse Source Localization Project
# ==============================================
# NOTE: FEniCSx is best installed via conda (see environment.yml)
# If using pip-only, you may need system-level dependencies:
#   Ubuntu/Debian: sudo apt install libopenmpi-dev openmpi-bin petsc-dev
#   macOS: brew install open-mpi petsc
#
# For pip installation of FEniCSx (may require additional setup):
# pip install fenics-dolfinx

# Core Scientific Stack
numpy>=1.24
scipy>=1.11
mpi4py>=3.1

# Mesh handling
gmsh>=4.12
meshio>=5.3
h5py>=3.9

# Visualization
matplotlib>=3.7
pyvista>=0.42

# Jupyter support
jupyterlab>=4.0
ipykernel
ipywidgets

# Utilities
tqdm

# Development
pytest
black
pylint

# For inverse problem solving (optimization)
scikit-learn>=1.3  # For DBSCAN clustering of intensity field
# cvxpy  # Convex optimization (optional)
